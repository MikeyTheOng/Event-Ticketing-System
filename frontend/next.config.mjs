/** @type {import('next').NextConfig} */

const nextConfig = {
    images: {
        domains: ['static.ticketmaster.sg', 's1.ticketm.net', 'prismic-images.tmol.io', 'www.billboard.com', 'res.klook.com', 'm.media-amazon.com', 'images.says.com', 'pbs.twimg.com', 'static.pollstar.com', 'www.arthipo.com'],
    },
};

export default nextConfig;
